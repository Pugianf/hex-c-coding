#include "holberton.h"
#include <stdio.h>

/**
 * print_line - prints n times '_', then creating a line on the screen
 *
 * @n: number of times that '_' must be printed
 */

void print_line(int n)
{
	int a;

	if
	{
		for (a = n; a > 0; a--)
		{
			_putchar(95);
		}
	}

	_putchar('\n');
}
